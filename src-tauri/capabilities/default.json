{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permissoes minimas da janela principal. Cada entrada abaixo tem justificativa em SECURITY.md; adicionar uma permissao exige revisao explicita (SPEC-010).",
  "windows": ["main"],
  "permissions": [
    "core:app:default",
    "core:event:default",
    "core:window:default",
    "core:webview:default"
  ]
}
